schema: ./*.graphql
extensions:
  codegen:
    generates:
      audio-api/src/generated-types.d.ts:
        plugins:
          - typescript
          - typescript-resolvers
  diff:
    baseSchema: your-base-schema-here
